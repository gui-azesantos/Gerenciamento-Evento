@model IEnumerable<GerenciamentoEvento.Models.Local>

    <h2>Locais</h2>
 

    <table id="local" class="table table-striped table-bordered">
        <thead style="background-color:#6E3667; color:grey; text-align: center">
            <th>
            Nome
            </th>
            <th>
             Endereço
             <th>
             <a asp-action="Criar" asp-controller="Local" class="btn btn-success" style="loat: right;"> Criar novo local </a> 
             </th>
             </thead>

    
<tbody>
    @foreach(var local in Model)
    {  
        <tr>
            
         <td>
            @local.Nome 
        </td>
         <td>
            @local.Endereco 
        </td>


        <td>
           <a href="/Local/Editar/@local.Id" class="btn btn-primary">Editar</a> 
        <a href="/Local/Deletar/@local.Id" class="btn btn-danger">Deletar</a> 
        </td>
        </tr>
       
    }
    </tbody>
</table>